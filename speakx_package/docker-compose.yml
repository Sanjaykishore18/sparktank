version: '3.8'

services:
  # MongoDB Database
  mongo:
    image: mongo:7
    container_name: speakx-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - speakx-network

  # Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: speakx-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
      - MONGODB_URI=mongodb://mongo:27017/speakx
      - JWT_SECRET=${JWT_SECRET:-speakx_jwt_secret_change_in_production}
      - JWT_EXPIRES_IN=7d
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - CLIENT_URL=http://localhost:3000
    depends_on:
      - mongo
    networks:
      - speakx-network
    restart: unless-stopped

  # Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: speakx-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - speakx-network
    restart: unless-stopped

volumes:
  mongo-data:

networks:
  speakx-network:
    driver: bridge
